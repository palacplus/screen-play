name: Semantic Commit

on:
  workflow_call:

jobs:
  add-semantic-commit:
    if: contains(github.event.label.name, 'major') || contains(github.event.label.name, 'minor') || contains(github.event.label.name, 'patch')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - name: Determine commit message based on label
        id: commit-message
        run: |
          LABEL="${{ github.event.label.name }}"
          case "$LABEL" in
            "major")
              echo message="BREAKING: Prepare for major release" >> $GITHUB_OUTPUT
              echo "✅ Added BREAKING commit for major version bump"
              ;;
            "minor")
              echo message="feature: Prepare for minor release" >> $GITHUB_OUTPUT
              echo "✅ Added feat commit for minor version bump"
              ;;
            "patch")
              echo message="bugfix: Prepare for patch release" >> $GITHUB_OUTPUT
              echo "✅ Added bugfix commit for patch version bump"
              ;;
          esac

      - name: Commit and push changes
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "${{ steps.commit-message.outputs.message }}"
          token: ${{ secrets.PAT_FOR_WORKFLOW }}

      - name: Add confirmation comment
        uses: actions/github-script@v6
        with:
          script: |
            const label = '${{ github.event.label.name }}';
            const keywords = {
              'major': 'BREAKING',
              'minor': 'feature', 
              'patch': 'bugfix'
            };
            
            const keyword = keywords[label];            
            let message;
            message = `🏷️ **${label.toUpperCase()} label applied**
            
            🤖 Added empty commit with "${keyword}" keyword to ensure proper version bumping.
            
            The semantic versioning action will now correctly identify this as a **${label}** version bump when this PR is merged.`;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: message
            });
